CREATE TABLE warehouses (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, name TEXT, latitude NUMERIC, longitude NUMERIC);
CREATE TABLE items (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, warehouse_id INTEGER, quantity INTEGER, FOREIGN KEY(warehouse_id) REFERENCES warehouses(id));