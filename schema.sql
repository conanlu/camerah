CREATE TABLE photos (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, name TEXT, user_id INTEGER, upvotes INTEGER, FOREIGN KEY(user_id) REFERENCES users(id))
CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, username TEXT NOT NULL, hash TEXT NOT NULL)